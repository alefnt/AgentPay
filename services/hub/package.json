{
  "name": "@agentpay/hub",
  "version": "0.1.0",
  "description": "AgentPay Hub Server — Managed Fiber node access for Agents",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/server.ts",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@agentpay/core": "workspace:*",
    "@agentpay/sdk": "workspace:*",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.0.0",
    "rimraf": "^6.0.0",
    "vitest": "^3.0.0",
    "@types/better-sqlite3": "^7.6.0"
  }
}
