{
  "name": "@agentpay/x402-facilitator",
  "version": "0.1.0",
  "description": "x402 Facilitator for AgentPay — Bridge between x402 protocol and CKB Fiber Network",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@agentpay/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.0.0",
    "rimraf": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
