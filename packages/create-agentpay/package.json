{
  "name": "create-agentpay",
  "version": "0.1.0",
  "description": "One-command AgentPay + Fiber node deployment â€?no development required",
  "type": "module",
  "bin": {
    "create-agentpay": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentpay-dev/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.0.0",
    "rimraf": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
